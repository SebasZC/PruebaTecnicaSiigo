pipeline {
    agent any
    tools {
        jdk 'jdk17'
    }
    stages {
        stage('RunTest') {
            steps {
                dir('C:/Users/Sebas/.jenkins/workspace/pipeline pruebas') {
                    bat 'gradle clean build -x test'
                }
            }
        }
    }
    post {
        success {
            echo 'Tests success'
        }
        failure {
            echo 'Tests Failed'
        }
    }
}
